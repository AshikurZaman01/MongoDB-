// foreach , find , filter , map , indexOf , reduce

const arr = [1, 2, 3, 4, 5];

// if i want to add some text in each data. then have to use foreach loop

// show output as data + " " + "hello"
const dd = arr.forEach((data) => {
    console.log(data + " " + "hello");
})